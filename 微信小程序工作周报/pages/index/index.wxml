<!--index.wxml-->
<!--<view class="container">
  <view class="userinfo">
    <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 获取头像昵称 </button>
    <block wx:else>
      <image bindtap="bindViewTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover"></image>
      <text class="userinfo-nickname">{{userInfo.nickName}}</text>
    </block>
  </view>
  <view class="usermotto">
    <text class="user-motto">{{motto}}</text>
  </view>
</view>-->
<view class="container">
  <view class="loginbox">
      <view class="userbox">
          <image class="userimg" src="../images/user.png"></image>
          <input class="username" placeholder='请输入用户名'  bindinput="getuserId"/>
      </view>
     <view class="userbox">
      <image class="userimg" src="../images/pwd.png"></image>
      <input type="password" class="pwd" placeholder='请输入密码'/>
     </view>                   
       <button class="btn" bindtap='bindTapLogin'>登录</button>      
  </view>
</view>

